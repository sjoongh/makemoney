__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.env
money.txt
.kis_token.json
.run_ledger.json
logs/
.omc/state/
paper_forward/
.kill_switch.json
# research_data/ is committed as a versioned dataset snapshot (daily OHLCV).
# Only transient/garbage artifacts are ignored:
research_data/_quarantine/
research_data/*.backup
research_data/*.tmp
experiments/

# OMC advisor scratch artifacts (Codex/Gemini consult outputs)
.omc/artifacts/

# Forward recorder output — point-in-time raw bars + membership log, grows
# daily via cron (machine-local; snapshot to git periodically like research_data).

# EDGAR fundamentals store (point-in-time, fetched — re-fetchable, not committed)

# Beta/defensive paper forward track (grows daily via cron)


# Forward stores: ignore the BULKY re-fetchable parts, BACK UP the small
# irreplaceable point-in-time logs (universe membership, fundamentals snapshot,
# beta track) — those are committed by the daily backup cron.
forward_data/*.parquet
forward_data/*.parquet.tmp
fundamentals_edgar/
!forward_data/_fundamentals.jsonl

# Beta KIS paper track (grows via cron)
beta_kis_track.jsonl

# KOSDAQ research data (re-fetchable via FDR)
kosdaq_data/
